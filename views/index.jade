extends layout

block content
  style
    label:hover { background: #ffb; }
    label { padding: 3px; margin: 2px; background: #eee; }
    .uncheck { color: #bbb; } .uncheck:hover { color: #000; }

  div.header(style="text-align: center; max-width: 800px; margin: 15px auto; border: 1px solid #ccc; background: #eee; padding: 10px 15px; border-radius: 10px")
    h1 Olin SEO Elections 2013
    p(style="font-size: 16px") Pretty straightforward kids: excellence votings. And some questions about size and color (of the t-shirt).
    p(style="font-style: italic") 
      | You are logged in as 
      b
        = user.id
      | . 
      a(href="/logout/")
        | Logout?

  if saved
    div.alert.alert-success(style="text-align: center") Your vote has been saved! You can recast it as often as you'd like before 10PM Wednesday, April 17th.
  else
    p(style="text-align: center") Cast your vote below. You can recast it as often as you'd like before 10PM Wednesday, April 17th.

  div.containers
    form(method='post')
      each submission in submissions
        .row
            h2 #{submission.name}
              br
              each url in submission.urls
                div.span3
                  img(src=String(url),width="600px",height="600px")
        //- each seo, seoname in positions
        //-   div.span3
        //-     h2 #{seoname}
        //-     p
        //-     each pos, posname in seo
        //-       div.position(style="margin-bottom: 20px")
        //-         h4= posname
        //-         each person in pos
        //-           label(style=(String(answers['vote-' + seoname + '-' + posname]).indexOf(person.name) > -1 ? "font-style: italic" : ""))
        //-             input(name='vote-' + seoname + '-' + posname, type='checkbox', value=person.name, checked=String(answers['vote-' + seoname + '-' + posname]).indexOf(person.name) > -1, style="vertical-align: baseline")
        //-             |  &nbsp; #{person.name}

            p(style="margin-top: 10px; line-height: 14px")
              small(style="font-size: 10px; color: #999") When finished, scroll down and click "cast your vote".
                  
      div(style="text-align: center; margin-top: 20px")
        button.btn.btn-success.btn-large.btn-primary(style="font-size: 30px; padding: 15px 30px", disabled) Cast Your Vote

    br